services:
  - type: web
    name: sast-report-generator
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd web && python app.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"